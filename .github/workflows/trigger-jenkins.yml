name: 🚀 Trigger Jenkins Build

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  trigger-jenkins:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Jenkins Job
        uses: appleboy/jenkins-action@master
        with:
          url: ${{ secrets.JENKINS_URL }}
          user: ${{ secrets.JENKINS_USER }}
          token: ${{ secrets.JENKINS_TOKEN }}
          # SỬA DÒNG DƯỚI NÀY THÀNH TÊN PROJECT CỦA BẠN TRÊN JENKINS
          job: "22120032"
          wait: true # GitHub sẽ đợi Jenkins chạy xong mới báo xanh
          timeout: "10m" # Nếu quá 10 phút thì hủy
